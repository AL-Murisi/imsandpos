// import { db } from "@/lib/indexedDB";

// async function saveOfflineOperation(op: OfflineOperationDB) {
//   await db.offlineOperations.add({
//     ...op,
//     status: "pending",
//     createdAt: new Date(),
//   });
// }
